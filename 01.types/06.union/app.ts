function combine(input1: number | string, input2: number | string) {
    return input1.toString() + input2
}

const comb1 = combine(4, 'ever')
const comb2 = combine('Gr', 8)

console.log(comb1)
console.log(comb2)